---

- name: Create local datafolders
  file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: 0777
  loop:
    - "/{{ prop_basename }}/bash"
    - "/{{ prop_basename }}/bash/scripts"
  vars:
    ansible_connection: "{{ prop_ansible_connection }}"
    ansible_host: "{{ prop_ipaddress }}"
  become: true

- name: Copy bash script
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    owner: root
    group: root
    mode: 0755
  loop:
    - src: "bash.sh.j2"
      dest: "/{{ prop_basename }}/bash/bash.sh"
    - src: "test.sh.j2"
      dest: "/{{ prop_basename }}/bash/scripts/test.sh"
  vars:
    ansible_connection: "{{ prop_ansible_connection }}"
    ansible_host: "{{ prop_ipaddress }}"
  become: true

- name: update bashrc
  shell: |
    if [[ $(cat {{ item }}/.bashrc | grep "ALEX.Bash" | wc -l ) == 0 ]] ; then
      echo ". /{{ prop_basename }}/bash/bash.sh  # ALEX.Bash " >> {{ item }}/.bashrc
    fi
  args:
    executable: /bin/bash
  loop:
    - "/root"
    - "/home/{{ prop_admin_username }}"
  vars:
    ansible_connection: "{{ prop_ansible_connection }}"
    ansible_host: "{{ prop_ipaddress }}"
  become: true



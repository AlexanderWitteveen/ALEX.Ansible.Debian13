---

- name: "Playbook: Build Debian 13 Base Template"
  hosts: "{{ args_host }}"
  gather_facts: false

  tasks:
    - ansible.builtin.include_role:
        name: alex.debian13.buildbasetemplate
